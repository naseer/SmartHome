listener 1883
persistence true
persistence_location /mosquitto/data/
log_dest stdout                  # container-native: view via `docker logs mosquitto` (no log-dir uid/perms trap)

# Security: no anonymous access. setup-masn.sh creates the user via mosquitto_passwd.
allow_anonymous false
password_file /mosquitto/config/passwd
