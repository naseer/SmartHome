# Snapcast server -- Phase 5 (speakers wired). Minimal stub; expand into multi-zone later.
[stream]
source = pipe:///tmp/snapfifo?name=default&codec=flac

[http]
enabled = true
doc_root = /usr/share/snapserver/snapweb

[tcp]
enabled = true
